{
  "v": "stozher/0.1",
  "description": "Language-neutral test vectors for the Stozher stozher/0.1 specification. Every vector carries its expected output; consuming test suites MUST read expected values from here and MUST NOT hardcode them.",
  "spec": "../00-overview.md",
  "encoding": {
    "binary": "lowercase-hex",
    "base64": "never used",
    "digest": "sha256, 64 lowercase hex characters",
    "signature": "ed25519, 128 lowercase hex characters",
    "public-key": "ed25519, 64 lowercase hex characters",
    "secret-key": "ed25519 seed, 64 lowercase hex characters (RFC 8032 private key)",
    "timestamp": "RFC 3339 UTC, exactly three fractional digits, Z suffix",
    "money": "decimal string, never a JSON number"
  },
  "dispatch": "Harnesses MUST dispatch on the `kind` member of each file (also mirrored per file in `files[].kind`) and MUST fail on an unrecognised kind rather than skipping it.",
  "files": [
    {
      "path": "jcs-canonicalization.json",
      "kind": "jcs",
      "role": "primitive",
      "count": 22,
      "description": "RFC 8785 canonicalization. Parse `input-json` (raw JSON text, so that member order, escapes and numeric literals are preserved exactly), canonicalize, and compare bytes against `canonical` and its SHA-256 against `canonical-sha256`."
    },
    {
      "path": "jcs-invalid.json",
      "kind": "jcs-invalid",
      "role": "primitive",
      "count": 13,
      "description": "Inputs a conformant canonicalizer MUST reject, with the normative error code (spec 01 section 3.1). Parsing `input-json` MUST fail with `error`."
    },
    {
      "path": "sha256.json",
      "kind": "sha256",
      "role": "primitive",
      "count": 8,
      "description": "SHA-256 known-answer tests. Hash the octets of `input-hex`; result MUST equal `sha256`."
    },
    {
      "path": "ed25519.json",
      "kind": "ed25519",
      "role": "primitive",
      "count": 8,
      "description": "Ed25519 sign/verify. When `secret-key` is present, signing the octets of `message-hex` MUST produce `signature` (Ed25519 is deterministic). In every vector, strict verification of `signature` over `message-hex` under `public-key` MUST return `verifies`."
    },
    {
      "path": "slip10-ed25519.json",
      "kind": "slip10-ed25519",
      "role": "primitive",
      "count": 11,
      "description": "SLIP-0010 hardened ed25519 derivation (spec 01 section 6). Derive `path` from `seed`; the results MUST equal `chain-code`, `private-key` and `public-key`. `slip10-public-key` is the same key with SLIP-0010's leading 0x00 byte, for cross-checking against published vectors; Stozher key identifiers use the 32-byte form."
    },
    {
      "path": "object-hash.json",
      "kind": "object-hash",
      "role": "primitive",
      "count": 3,
      "description": "The signed-object pattern of spec 01 section 5. `expected-jcs` is JCS(object); `expected-object-hash` is its SHA-256 (the object's id, sig included); `expected-signing-input` is JCS(object minus sig)."
    },
    {
      "path": "envelope.json",
      "kind": "envelope",
      "role": "primitive",
      "count": 6,
      "description": "Envelope hashing and signature verification (spec 02). `envelope-hash` is object-hash over the complete signed envelope and is the value referenced by prev-hash."
    },
    {
      "path": "envelope-shape.json",
      "kind": "envelope-shape",
      "role": "primitive",
      "count": 76,
      "description": "Structural validation of envelopes (spec 02 section 9). Validate `envelope`; the result MUST match `expected.valid`, and on failure the reported code MUST equal `expected.error`. Signature verification is NOT part of this check except where the vector's error code says so."
    },
    {
      "path": "chain.json",
      "kind": "chain",
      "role": "primitive",
      "count": 8,
      "description": "Hash chain construction and verification (spec 04 section 2). Verify the envelopes in order: signature, seq continuity, prev-hash linkage, stream identity. On success return the head hash. Verification MUST NOT consult evidence payloads -- none are supplied here, and the expected head hash is the same one the payload-binding vectors produce with payloads present."
    },
    {
      "path": "mandate-chain.json",
      "kind": "mandate-chain",
      "role": "primitive",
      "count": 26,
      "description": "Mandate chain verification (spec 03 section 5). Resolve `leaf-ref` in `mandates`, then walk to a human root using `roots`, `revocations`, `at`, `subject-key`, `max-delegation-depth` and either `request` (the full tuple of section 4.2) or `resource-only` (the cognition form: match `resources` alone, the other three dimensions unconstrained). `max-delegation-depth`. On success `expected.human-root` and `expected.depth` MUST match; on failure `expected.error` MUST match."
    },
    {
      "path": "authorization.json",
      "kind": "authorization",
      "role": "primitive",
      "count": 23,
      "description": "Gate authorization verification (spec 06 section 2). Verify `envelope.authorization` given `requires-gate`, `approvers` and the set of already-seen request hashes. This is the file that makes 'approved is not a boolean' testable."
    },
    {
      "path": "payload-binding.json",
      "kind": "payload-binding",
      "role": "primitive",
      "count": 7,
      "description": "Evidence payload binding and decay (spec 04 section 5). Validate the ingest record: every submitted payload MUST hash to the value the envelope committed to, and MUST be referenced by it. An empty `payloads` array is always valid. Where a vector carries `chain`, verifying that chain with no payloads MUST produce `expected.chain-head-hash`."
    },
    {
      "path": "money-compare.json",
      "kind": "money-compare",
      "role": "primitive",
      "count": 31,
      "description": "Exact comparison of monetary decimal strings. `expected` is one of less, equal, greater, refused. A harness MUST compare by value without converting to a binary floating-point number, and MUST refuse every form outside `digits [ '.' digits ]`."
    },
    {
      "path": "parity.json",
      "kind": "parity",
      "role": "primitive",
      "count": 16,
      "description": "Cross-implementation parity (spec 01 section 2.3, 04 sections 2-3, 06 sections 1.2, 2 and 5). Each vector reaches a branch on which two independent implementations of this specification were observed to disagree. Dispatch on `algorithm`: `verify-authorization` takes `input.{envelope,requires-gate,approvers,seen-request-hashes}` and `verify-chain` takes `input.{stream,envelopes,expected-first-prev}`. Note that `approvers` entries here are objects carrying a `key` and a nullable `subject`, not bare key strings: §06 §5 states self-approval over the subject as well as the key, and a verifier given only keys cannot evaluate it. `divergence` is documentation and MUST NOT be asserted on."
    },
    {
      "path": "policy-evaluation.json",
      "kind": "policy-evaluation",
      "role": "primitive",
      "count": 14,
      "description": "Classification and the gate rule (spec 05 section 3). Evaluate `request` against `policy`: the resulting class MUST equal `expected.class` and the gate rule's verdict MUST equal `expected.decision`. `request.manifest-class` is the class a registered manifest proposes, or null when the component has none."
    },
    {
      "path": "checkpoint.json",
      "kind": "checkpoint",
      "role": "kernel",
      "count": 6,
      "description": "Checkpoint attestation (spec 04 section 4). Verify `checkpoint` against `range`, anchoring the first envelope with `expected-first-prev` when it is not null. The result MUST match `expected.valid`; on failure the code MUST equal `expected.error`; on success the head hash and `anchored` MUST match. No payload is read."
    },
    {
      "path": "trigger.json",
      "kind": "trigger",
      "role": "kernel",
      "count": 6,
      "description": "Trigger resolution (spec 07 section 4). `mandate` is the mandate object the envelope's `mandate-ref` resolves to; `appended-signals` lists the signal envelope ids the store holds. Apply rules 1 to 3; the result MUST match `expected.valid` and on failure the code MUST equal `expected.error`. Structural validation of the envelope is not part of this check."
    },
    {
      "path": "manifest.json",
      "kind": "manifest",
      "role": "kernel",
      "count": 17,
      "description": "Manifest validation (spec 08 section 1). Validate `manifest` as a signed object and against section 1's rules; the result MUST match `expected.valid` and on failure the code MUST equal `expected.error`. Registration conditions (section 3) are not part of this check."
    },
    {
      "path": "gate-arguments.json",
      "kind": "gate-arguments",
      "role": "primitive",
      "count": 11,
      "description": "spec 06 section 4.4 rules 3 and 4 — argument values submitted beside a parked request. Canonicalize `arguments`; if the canonical form exceeds 16384 BYTES the result is `gate-arguments-too-large`; otherwise if its SHA-256 differs from `args-hash` the result is `gate-arguments-hash-mismatch`; otherwise `accept`. `canonical-bytes` is the length that decides the first test, and is given so a harness measuring characters fails visibly."
    },
    {
      "path": "gate-admission.json",
      "kind": "gate-admission",
      "role": "kernel",
      "count": 11,
      "description": "spec 06 section 4.4 rule 9 and spec 09 section 7 — the order in which a gate submission is refused, and which refusal earns a durable record. Run the admission predicate over `input`; `expected.outcome` is `queued`, `already-queued`, or the reason code the refusal carries, `expected.status` is the HTTP status, and `expected.record` is whether the kernel MUST write a section 04 rule 7.1 rejection-stream record for it. `arguments-check` is the section 4.4 rule 3/4 result over the submitted values (`null` when the member was omitted); `mismatches-in-window` counts the subject's OWN recorded argument mismatches in the window, which is not `parked-in-window` and is zero for a component that only ever lies."
    },
    {
      "path": "gate-resubmission.json",
      "kind": "gate-resubmission",
      "role": "primitive",
      "count": 12,
      "description": "spec 06 section 4.2 — re-submitting a request nobody has answered. Given the requests a component `held` and the `call` it is about to gate at `now`, `expected.resubmit` is the `request-hash` it MUST resolve to and `expected.decided` the answered request it MUST consume instead; `null` means it MUST park the request under `minted`. Identity is the nine fields of `call`, never `request-hash` — `minted.request-hash` differs from every held hash in every vector, which is what `nonce` (section 1.1) is for. A held request is eligible only while `state` is `pending` and `now` has not passed its `not-after`."
    },
    {
      "path": "root-change.json",
      "kind": "root-change",
      "role": "kernel",
      "count": 7,
      "description": "Root set changes (spec 03 section 6). Read the change out of `envelope` with `payloads`. The result MUST match `expected.valid`; on failure the code MUST equal `expected.error`; on success `expected.change` says whether it enrols or retires, `expected.key` is the key affected, and for an enrolment `expected.subject` is the human it MUST be recorded under. Who may make the change is not asked here — that needs the deployment's current root set."
    },
    {
      "path": "sync-outcome.json",
      "kind": "sync-outcome",
      "role": "primitive",
      "count": 16,
      "description": "spec 05 section 7.1 — what a component may do when its last submission was `accepted`, `unreachable` or `refused`. Run the predicate over `input`; `expected.action` is `serve` or `refuse`, `expected.reason-code` is the code a refusal carries verbatim, and `expected.finding` is whether serving it must also be recorded as a finding. A `refused` submission is NOT the offline case: the offline map governs a kernel that cannot answer, never one that has answered no."
    },
    {
      "path": "stream-status.json",
      "kind": "stream-status",
      "role": "kernel",
      "count": 9,
      "description": "spec 09 section 4.2 — the predicate behind the row a console renders for one stream. `refused` when the most recent thing that happened was a refusal, whatever the quiet interval says; `quiet` when nothing has been accepted within `quiet-after-seconds`; `healthy` otherwise. Which row an implementation draws is its own business; the predicate is not."
    },
    {
      "path": "stream-recovery.json",
      "kind": "stream-recovery",
      "role": "kernel",
      "count": 7,
      "description": "spec 04 section 7.2 — resuming a stream wedged by a refusal. Read the resume out of `envelope` with `payloads`: the result MUST match `expected.valid`, on failure the code MUST equal `expected.error`, and on success `expected.stream`, `expected.resume-seq` and `expected.bridge-prev-hash` are the position authorized and the `object-hash` of the refused bytes that bridges it. Where `chain` is present, verifying it against `expected-first-prev` MUST succeed and return `expected.chain-head-hash` — the emitter's own chain, unrenumbered, continuing past a position that stays refused."
    }
  ]
}
